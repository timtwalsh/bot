version: '3.8'

services:
  bot:
    build: .
    env_file:
      - ./.env
    environment:
      - TZ=Australia/Brisbane
    volumes:
      - bot_data:/app/data
    restart: unless-stopped

volumes:
  bot_data: